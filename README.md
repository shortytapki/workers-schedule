# Деплой приложения

## Запуск с помощью node

```bash
    npm install
    npm run serve
```

## Запуск с помощью docker

```bash
    docker compose up -d
```

Проект будет доступен на http://localhost:3000
